for (yield _a; yield _b; yield _e) yield

<container:for>
  <container:next>
    <seg l>yield e</seg>
  </container>
  <seg:init a>yield a</seg>
  <seg:test b>yield b</seg>
  <seg e>yield</seg>
</container>

if state <= l {
  while !false {
    if state >= l { state = a; return _e }
    if state == a { state = b; return _a }
    if state == b { state = e; return _b }
    if state == e { state = l; return }
  }
}    
