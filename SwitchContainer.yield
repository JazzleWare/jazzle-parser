switch (yield) {
   case yield a: a() // if ( m || (m = (yield a) == v) ) a()
   case yield b: b() // if ( m || (m = (yield b) == v) ) b()
}

m = 0
v = yield
if m || (m = (yield a) == v): a()
if m || (m = (yield b) == v): b()


